// keyboard shortcuts
// keyblindings doesn't work for workspace, copy this file to user settings to work
// to remove the default shortcut, prepend '-' to the command. example: `{"command": "-editor.action.deleteLines",...}`
[
  {
    // duplicate selection
    "key": "ctrl+d",
    "command": "editor.action.duplicateSelection"
  },
  {
    // select the next similar
    // to select all semialr : ctr+shift+L
    "key": "alt+d",
    "command": "editor.action.addSelectionToNextFindMatch"
  },
  // toggle blocK comments
  // same as toggle line comment (i.e. ctrl+/) plus shift
  {
    "key": "ctrl+shift+/",
    "command": "editor.action.blockComment",
    "when": "editorTextFocus && !editorReadonly"
  },
  {
    // `ctrl+shift+k` is used to delete line
    "key": "-ctrl+shift+k",
    "command": "tsce.showCodeExplorer"
  }
]
